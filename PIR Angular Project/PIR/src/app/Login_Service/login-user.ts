//Model which is Used to Define the Structure of Json Format
//Used to hold the Json information which we are getting during API call

export class LoginUser {
    id:number;
    email_id:String;
    reporter_Id:String;
    reporterName:String;
    is_ADMIN:String;
}
